<?php

/**
 *
 * write any custom code in this class, build operation wont overwrite this class once generated;
 */

namespace module\documentation\model;

class topics_domain_logic extends \data_model {
    /*
      public function afterSave($created) {
      parent::afterSave($created);
      $relatedTopics = \module\documentation\model\related_topics::getInstance();
      if (select("id")->from($relatedTopics)->inserted()
      ->where('topic_id', $this->id)
      ->limit(1)
      ->execute()->fetch(\PDO::FETCH_COLUMN, 0) == false) {
      $relatedTopics->saveModel(array("topic_id" => $this->id, 'parent_id' => 0), array('atomic' => true));
      }
      }
     */
}